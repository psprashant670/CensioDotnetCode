
@{ var hrefURL = Censio_CMS.Startup.GethrefURL();}
@model IEnumerable<Domain.Entities.TblGame3FestivalReset>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="block full">
    <div class="block-title">
        <h2><strong> @ViewBag.PageName</strong> </h2>
    </div>
    <div class="table-responsive">

        <button type="button" onclick="location.href='@Url.Action("Create", "Game3FestivalReset")'" class="btn btn-sm btn-primary"><i class="fa fa-plus"></i>Add</button>
        <table id="example-datatable" class="table table-vcenter table-condensed table-bordered">
            <thead>
                <tr>
                    <th class="text-center">Sr No.</th>
                    <th class="text-center">Festival Id</th>
                    <th class="text-center">Festival Name</th>
                    <th class="text-center">Past Festival Reset</th>
                    <th class="text-center">Future Festival Reset</th>
                    <th class="text-center">Status</th>
                    <th class="text-center">Updated Date Time</th>
                    <th class="text-center">Actions</th>


                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="text-center">@Html.DisplayFor(modelItem => item.IdFestivalResetLog)</td>
                        <td class="text-center">@Html.DisplayFor(modelItem => item.IdFestival)</td>
                        <td class="text-center">@Html.DisplayFor(modelItem => item.FestivalName)</td>
                        <td class="text-center">@Html.DisplayFor(modelItem => item.PastFestivalReset)</td>
                        <td class="text-center">@Html.DisplayFor(modelItem => item.FutureFestivalReset)</td>
                       @if (item.Status == "A")
                        {
                            <td class="text-center">Active</td>
                        }
                        else
                        {
                            <td class="text-center"> DeActive</td>
                        }
                       
                        <td class="text-center">@Html.DisplayFor(modelItem => item.UpdatedDateTime)</td>

                        <td class="text-center">
                            <div class="btn-group">
                                @Html.ActionLink("Edit", "Edit", new { id = item.IdFestivalResetLog }, new { @title = "Edit", @class = "glyphicon glyphicon-pencil" })
                                @*<a href="javascript:void(0)" onclick="GetAddEdit(@item.Id_Industry);" data-toggle="tooltip" title="Edit" class="btn btn-effect-ripple btn-xs btn-success"><i class="material-icons">create</i></a>*@
                                @if (item.Status == "A")
                                {
                                    @Html.ActionLink("DeActivate", "DeActivate", new { id = item.IdFestivalResetLog }, new { @title = "DeActivate", @class = "glyphicon glyphicon-pencil" })
                                }
                                else
                                {
                                    @Html.ActionLink("Activate", "Activate", new { id = item.IdFestivalResetLog }, new { @title = "DeActivate", @class = "glyphicon glyphicon-pencil" })
                                }

                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<script src="~/AppJavascript/Game3FestivalReset.js"></script>